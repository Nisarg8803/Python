def SWAP (B):
    OUTPUT = '';
    for PARAGRAPH in B:
        if PARAGRAPH.isupper()==True:
            OUTPUT += (PARAGRAPH.lower())
        else:
            OUTPUT += (PARAGRAPH.upper())
    return OUTPUT
